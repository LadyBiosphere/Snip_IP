<!-- Content strictly for accessibility -->
<div id="container">

		<!-- Sections highlighting SNIP -->
	<div class="news-container">

		<!-- SNIP Mission -->
		<div class="news">
			<h3>Keyboard Shortcuts</h3>
			<hr/>
			<p>You can jump directly to specific pages using the following shortcuts:</p>
		</div>
	</div>

	<div class="prices-container">

			<table class="access-board">
				<thead>
					<tr>
						<th>Page Name</th>
						<th>Chrome, I.E, Safari (Windows)</th>
						<th>Firefox (Windows)</th>
						<th>Safari, Chrome, Firefox (Mac)</th>
					</tr>
				</thead>
				<tbody>
					<tr>
						<td><a href="index.php?page=home">Home</a></td>
						
						<td><code>alt</code> + <code>1</code></td>
						<td><code>shift</code> + <code>alt</code> + <code>1</code></td>
						<td><code>ctrl</code> + <code>option</code> + 1</td>
					</tr>
					<tr>
						<td><a href="index.php?page=news">Latest News</a></td>
						
						<td><code>alt</code> + <code>2</code></td>
						<td><code>shift</code> + <code>alt</code> + <code>2</code></td>
						<td><code>ctrl</code> + <code>option</code> + 2</td>
					</tr>
					<tr>
						<td><a href="index.php?page=learn">Learn about Us</a></td>
						
						<td><code>alt</code> + <code>3</code></td>
						<td><code>shift</code> + <code>alt</code> + <code>3</code></td>
						<td><code>ctrl</code> + <code>option</code> + 3</td>
					</tr>
					<tr>
						<td><a href="index.php?page=team">Meet the Team</a></td>
						
						<td><code>alt</code> + <code>4</code></td>
						<td><code>shift</code> + <code>alt</code> + <code>4</code></td>
						<td><code>ctrl</code> + <code>option</code> + 4</td>
					</tr>
					<tr>
						<td><a href="index.php?page=prices">Check out our prices</a></td>
						
						<td><code>alt</code> + <code>5</code></td>
						<td><code>shift</code> + <code>alt</code> + <code>5</code></td>
						<td><code>ctrl</code> + <code>option</code> + 5</td>
					</tr>
					<tr>
						<td><a href="index.php?page=contact">Contact Us</a></td>
						
						<td><code>alt</code> + <code>6</code></td>
						<td><code>shift</code> + <code>alt</code> + <code>6</code></td>
						<td><code>ctrl</code> + <code>option</code> + 6</td>
					</tr>
					<tr>
						<td><a href="index.php?page=access">Accessibility</a></td>
						
						<td><code>alt</code> + <code>7</code></td>
						<td><code>shift</code> + <code>alt</code> + <code>7</code></td>
						<td><code>ctrl</code> + <code>option</code> + 7</td>
					</tr>
					<tr>
						<td><a href="http://goo.gl/dAL9HL">Facebook</a></td>
						
						<td><code>alt</code> + <code>8</code></td>
						<td><code>shift</code> + <code>alt</code> + <code>8</code></td>
						<td><code>ctrl</code> + <code>option</code> + 8</td>
					</tr>
					<tr>
						<td><a href="http://goo.gl/ba8OKF">Collect</a></td>
						
						<td><code>alt</code> + <code>9</code></td>
						<td><code>shift</code> + <code>alt</code> + <code>9</code></td>
						<td><code>ctrl</code> + <code>option</code> + 9</td>
					</tr>
					<tr>
						<td><a href="index.php?page=login">Admin Login</a>/<a href="index.php?page=logout">Logout</a></td>
						
						<td><code>alt</code> + <code>0</code></td>
						<td><code>shift</code> + <code>alt</code> + <code>0</code></td>
						<td><code>ctrl</code> + <code>option</code> + 0</td>
					</tr>
				</tbody>
			</table>
		</div>
	</div>
<!-- End of written content -->